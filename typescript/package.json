{
  "name": "tech-challenge-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "./*"
  ],
  "type": "module",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "build": "yarn workspaces foreach run build",
    "test:unit": "yarn workspaces foreach run test:unit",
    "test:e2e": "yarn workspaces foreach run test:e2e"
  },
  "devDependencies": {
    "@captive/ts-config": "2.0.2",
    "@types/jest": "^29.0.0",
    "@types/node": "^16.0.0",
    "es-jest": "2.1.0",
    "jest": "29.7.0",
    "typescript": "5.3.3"
  },
  "packageManager": "yarn@4.0.2"
}
